Follow `AGENTS.md` as the repository-wide C#/.NET contract. Before completing a
change, run or reason through the active `DASxxxx`, `CAxxxx`, and `ASPxxxx`
diagnostics. Prefer semantic, cancellable, bounded, server-side operations.
Do not silence an analyzer to make generated code look complete.
